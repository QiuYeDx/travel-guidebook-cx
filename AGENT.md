# AI Agent 开发指南

## 项目定位

本仓库是“川西同行路书”，服务 2026 年中秋、国庆期间的川西自驾路线讨论与旅行中执行。
核心用户为 3 人，计划驾驶蔚来 EC6，2026 年 9 月 27 日从深圳出发，10 月 6 日回到深圳。

## 开始工作前

涉及中大型功能时先阅读：

- `docs/开发设计文档/20260825_chuanxi_roadbook/chuanxi_roadbook_final_design.md`
- `docs/开发设计文档/20260825_chuanxi_roadbook/chuanxi_roadbook_execution_plan.md`

查看进度台账，只认领一个最小工作包；完成后更新台账并写实施记录。

## 最高优先级规则

- 包管理器固定为 `pnpm@8.7.0`，`pnpm-lock.yaml` 必须保持 lockfile v6 兼容。
- 不要直接运行不确定版本的全局 `pnpm`。优先使用 `npx --yes pnpm@8.7.0 <command>`。
- 回答结束前必须关闭本轮启动的全部前端服务进程。
- 不把路线距离、车程、票务、天气、路况和补能状态伪装成实时精确数据。
- 动态信息必须包含 `verifiedAt` / 复核日期和下一次复核节点。
- 不在仓库里记录身份证号、手机号、详细病史、精确实时位置等敏感信息。
- 真实驾驶导航以车机和地图应用为准，项目只提供计划、上下文和跳转入口。

## 内容职责

```text
content/guidebook/  正式攻略与稳定讨论基线
content/notes/      讨论纪要、预订分工、临时复核记录
```

- 正式攻略的路线调整需要写调整原因、触发条件和复核日期。
- 临时信息先进入 `notes/`，确认后再合并到正式攻略或结构化数据。
- 最终路线必须唯一；降级动作必须说明何时执行，避免旅途中现场争论。
- 医疗内容只提供常识性风险识别和求助建议，不诊断、不替代医生意见。

## 前端边界

- 使用 Next.js App Router、React、TypeScript 和 Tailwind CSS 4。
- 默认使用 Server Component。需要 hooks、浏览器 API、localStorage 或交互时才使用 `"use client"`。
- `app/layout.tsx` 保持薄，只负责 metadata、全局样式、Provider 和 App Shell。
- 业务组件放入后续 `features/` 目录；`components/ui/` 只放 shadcn/ui 原子组件。
- qiuye-ui 组件放在 `components/qiuye-ui/`，安装前确认项目确实会使用。
- 优先使用 lucide 图标；不要手绘已有图标。
- 不接入需要密钥的地图、CMS、认证、数据库或分析服务，除非工作包和设计明确要求。

## 体验约束

- 移动端和弱网是核心场景，桌面端主要用于行前讨论。
- 旅行中首页优先展示“今天、下一段、风险、补能、住宿和紧急降级”，不做营销首页。
- 清楚区分稳定信息、季节信息和实时信息。
- 任何离线能力都要验证断网后可访问的页面和更新提示，不能只安装 PWA 外壳。
- 地图不可成为阅读路线的唯一方式；每日路线必须有可扫描的文本和时间线。
- 尊重 `prefers-reduced-motion`，关键状态不能只依赖颜色表达。

## 验证

常规验证使用固定版本：

```bash
npx --yes pnpm@8.7.0 lint
npx --yes pnpm@8.7.0 build
```

如需视觉验证，可以启动开发服务，但必须记录 PID，并在回答结束前结束该进程。

内容变更至少检查：

```bash
rg "待确认|复核|verifiedAt" content docs
```

检查 Markdown 表格、日期、路线方向、住宿夜数和降级动作是否互相矛盾。

## 禁止事项

- 禁止用新版 pnpm 更新旧 lockfile。
- 禁止把动态充换电站点列表硬编码成长期可靠数据。
- 禁止把社交媒体游记当作唯一事实来源。
- 禁止把高反处理写成“吸氧后继续赶路”等鼓励冒险的方案。
- 禁止在未核对现有用户改动时做大范围重构。
- 禁止启动前端服务后遗留进程。
